/** @menu {import('next').NextConfig} */
// const nextConfig = {}
const nextConfig = {
    // experimental: {
    //     appDir: true,
    // },
    // output: 'export',
};

module.exports = nextConfig
